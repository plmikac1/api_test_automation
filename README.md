# NASA NeoWs Test Framework

Framework automatyzacji testów API NASA Asteroids z uniwersalnymi metodami HTTP.

## Setup
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
pip install -e ".[dev]"
